# Changelog

## [3.5.0] - 2026-09-07

### 新增
- 独立于文字窗口的短期图片索引，保留发送者、消息 ID、时间和多图位置。
- 明确视觉提问自动带入同发送者近期唯一单图；原生图片和引用优先，歧义交由模型选择。
- `context_aware_view_images` 返回真实多模态图像，支持多图和 auto/high 清晰度升级。
- 有界后台预取与内存缓存，默认每会话 20 张、30 分钟、全局 64 MB；reset/new 与旧事件代际隔离。

### 性能与兼容
- 自动图使用临时内容标记；当前 Core 的工具图可能在本轮落库，下次请求精确移除本工具图像，保留文字与工具调用关系。
- 同轮重复查看去重、失败停止重试、不同图片总数受限；只允许公网 HTTP(S) 下载，重定向与 DNS 地址均校验。
- 视觉副本默认最长边 1600，high 为 4096；保留原始消息和原图。GIF 可选首帧，透明背景合成为白色。
- 最低 AstrBot 版本提高至 4.26.8；第三方 Agent runner 需单独验证。
- CI 执行 Python 3.10/3.12/3.13 单元测试与 Ruff，增加真实 AstrBot 工具循环集成验证脚本。

## 3.4.4 - 2026-08-01

- 历史图片预处理结果保持为自包含 `data URI`，不再把一小时缓存路径写回持久化会话。
- 自动移除已经失效的历史本地图片引用，避免 NewAPI 将路径误按 Base64 解码并导致整轮请求失败。
- 修正 `Image` 组件的 `file URI`/本地 `path` 语义，并复用规范化路径避免同一图片重复压缩。
- 新增历史 GIF、小图、过期缓存路径和组件引用语义回归测试。

## 3.4.3 - 2026-07-31

- LLM 请求图片预处理覆盖持久化历史 `contexts`，旧图片不再绕过压缩链。
- GIF 在请求侧提取首帧为 PNG 临时副本，兼容不支持 `image/gif` 的模型。
- 保留原始图片和会话历史，仅替换本次模型请求中的图片引用。

## 3.4.2 - 2026-07-31

- 将引用消息中真实内容为图片的 `File` 归一化为 `Image`，避免 Core 重复回查 OneBot。
- 支持 Pillow 可识别的 PNG、JPEG、WebP、GIF、BMP、TIFF、ICO 等图片格式。
- 非图片、损坏文件和伪图片后缀保持原文件组件，不阻断正常消息处理。
- 复用事件级图片压缩缓存，避免消息钩子和 LLM 请求钩子重复编码同一张图片。

## 3.4.1 - 2026-07-29

- 修复主动回复触发时大图未进入压缩流程的问题。
- 修复多图消息只转述第一张图片的问题。
- 正确放通图片下载、转述和压缩流程中的 `CancelledError`。
- 为图像转述缓存增加 1 小时 TTL，避免 CDN 链接过期后复用旧描述。
- 新增 `remove_message_async` 和 `remove_last_bot_response_async` 公共 API。
- 将 PNG 压缩等级从 9 调整为 6，降低压缩耗时。

## 3.4.0 - 2026-07-29

- 新增可配置的 LLM 请求图片压缩，仅生成临时副本，不修改原图。
- 支持按文件大小或最长边触发，并可配置质量、目标大小和输入上限。
- 加固 QQ CDN 图片下载的完整性校验、重试和失败缓存。

## 3.3.3 - 2026-07-28

- 修复执行 `/new` 和 `/reset` 后插件上下文未清理的问题。
- 兼容第三方 Agent runner 和 `astrbot_plugin_cmdmask` 的真实指令目标。

## 3.3.1 - 2026-07-27

- 修复 data URI 图片转述触发 `[Errno 36]` 的问题。
- 补充 data URI 和相关边界场景测试。

## 3.2.0 - 2026-07-26

- 减少群聊对话对象误判。
- 新增 `strict_mode` 配置及对应回归测试。

## 3.1.6 及更早版本

- 历史版本变更详见 Git 提交记录。
